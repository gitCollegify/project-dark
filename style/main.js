const sideNavLight ={
    backgroundColor:'orange',
    color: '#ccc',
    width: '30%',
    minHeight: '800px'
}
const sideNavDark ={
    backgroundColor:'#111',
    color: '#fff',
    width: '30%',
    minHeight: '800px'
}

const profileLight = {
    borderBottom: '2px solid #000',
    minHeight: '200px',
    width: '100%',
    color: '#111',
}

const profileDark = {
    borderBottom: '2px solid #fff',
    minHeight: '200px',
    width: '100%',
    color: '#fff',
}

const profileImg ={
    float: 'left',
    maxWidth: '150px',
    borderRadius: '50%',
    margin:'5%',
}

const navLight = {
    fontSize: '40px',
    color: '#000',
    listStyle:'none',
    textAlign:'center',
    lineHeight:2,
    cursor:'pointer'
}
const navDark = {
    fontSize: '40px',
    color: '#fff',
    listStyle:'none',
    textAlign:'center',
    lineHeight:2,
    cursor:'pointer'
}

const homeLight = {
    textAlign:'center',
    flexGrow: 1,
    color: '#111',
    backgroundColor: '#c2adcc',
}
const homeDark = {
    textAlign:'center',
    flexGrow: 1,
    color: '#fff',
    backgroundColor: '#250435',
}

const aboutLight = {
    textAlign:'center',
    flexGrow: 1,
    color: '#111',
    backgroundColor: '#a1d8f0',
}
const aboutDark = {
    textAlign:'center',
    flexGrow: 1,
    color: '#fff',
    backgroundColor: '#003519',
}

export {sideNavLight, sideNavDark, profileLight, profileDark, profileImg, navDark, navLight, homeLight, homeDark, aboutLight, aboutDark}